# coding=utf-8
# Importere json, så vi kan læse vores json fil
import json

class Kaffe:
    def __init__(self, kaffe, maelk, vand):
        self.kaffe = kaffe
        self.maelk = maelk
        self.vand = vand